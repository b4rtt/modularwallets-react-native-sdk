const { withPlugins } = require("@expo/config-plugins");

const withCircleModularWallets = (config) => {
  // Copy the implementation from the SDK's plugin
  // ...

  return config;
};

module.exports = withCircleModularWallets;
